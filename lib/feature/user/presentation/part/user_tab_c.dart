import 'package:flutter/widgets.dart';

class UserTabC extends StatelessWidget {
  const UserTabC({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('c'),
    );
  }
}
