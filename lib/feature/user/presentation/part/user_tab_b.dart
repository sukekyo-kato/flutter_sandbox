import 'package:flutter/widgets.dart';

class UserTabB extends StatelessWidget {
  const UserTabB({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('b'),
    );
  }
}
