import 'package:flutter/widgets.dart';

class UserTabA extends StatelessWidget {
  const UserTabA({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('a'),
    );
  }
}
